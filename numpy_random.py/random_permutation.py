import numpy as np
from numpy import random

# 1. shuffle : it will just change the position of array element.also the original array will not be affected 

# arr = np.array([1,2,3,4,5])
# random.shuffle(arr)
# print(arr)


# 2. permutaion : same as shuffle, it also simply change the position but not the original array
# arr = np.array([1,2,3,4,5])
# print(random.permutation(arr))